pygist
- gist create script
- Hirotaka Kawata <info@techno-st.net>
- http://www.techno-st.net/wiki/pygist

setup:
You should set github API token.
github => Account Settings => Administrative Information
$ git config --global github.user `Username`
$ git config --global github.token `API Token`

usage:
python pygist.py [filename ...]
  or
ln -s /path/to/pygist.py /usr/local/bin/gist
gist [filename ...]
